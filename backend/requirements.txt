# ── Flask API ──────────────────────────────────────────────────────────────────
flask==3.0.3
flask-cors==4.0.1
Pillow==10.3.0
numpy==1.26.4

# ── ViT model (PyTorch + HuggingFace)  [RECOMMENDED] ─────────────────────────
torch==2.3.0           # or torch==2.3.0+cu121 for CUDA 12.1 GPU
torchvision==0.18.0
transformers==4.41.2
accelerate==0.30.0
gunicorn==22.0.0
# ── CNN model (Keras / TensorFlow)  [fallback only] ──────────────────────────
# Uncomment if you want CNN fallback support:
# tensorflow==2.16.1

# ── Training script dependencies ──────────────────────────────────────────────
# (only needed to run pneumonia_vit.py, not the Flask server itself)
# scikit-learn==1.5.0
# matplotlib==3.9.0
# seaborn==0.13.2
# pandas==2.2.2