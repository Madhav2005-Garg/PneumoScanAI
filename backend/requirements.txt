# ── Flask API ──────────────────────────────────────────────────────────────────
flask==3.0.3
flask-cors==4.0.1
Pillow==10.3.0
numpy==1.26.4

# ── ConvNeXt model (PyTorch + TorchVision) ───────────────────────────────────
# torchvision ships the ConvNeXt-Tiny backbone + pretrained weights
torch==2.3.0            # or torch==2.3.0+cu121 for CUDA 12.1 GPU
torchvision==0.18.0     # must match torch version exactly